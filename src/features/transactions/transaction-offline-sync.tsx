"use client";

/** @deprecated Retry lives in ConnectivityRetryListener; UI in Connectivity* chips. */
export { ConnectivityRetryListener as TransactionOfflineSync } from "@/features/offline/connectivity-chip";
